command:
  - bash
  - -c
  - |
      echo "Current Directory:"
      pwd

      echo "Project Files:"
      ls -la

      cd frontend_part

      npm install
      npm run build

      echo "Dist Folder:"
      ls -la dist

      cd ..

      uvicorn backend.main:app --host 0.0.0.0 --port 8000